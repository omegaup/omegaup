declare namespace omegaup {
  export interface Identity {
  	name: string;
  	username: string;
  	school: string;
  	school_id: number;
  	country_id: string;
  	state_id: string;
  };
}

export default omegaup;